# 서버 설정
server.address=0.0.0.0
server.port=8080

# 이메일 설정
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=keonryul48@gmail.com
spring.mail.password=rpue fafj epyj pyks
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# 로깅 설정
logging.file.name=logs/application.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.level.root=INFO
logging.level.com.api.cookduck=DEBUG

# Swagger UI 설정
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=method

# MariaDB 설정 (Docker 컨테이너 사용)
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3307/cookduck?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=cookduck_user
spring.datasource.password=cookduck_password

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# H2 콘솔 설정
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# JWT 설정
jwt.secret=your-very-long-and-secure-secret-key-should-be-at-least-32-characters-long
jwt.expiration=86400000

# HTTP 인코딩 설정
spring.http.encoding.enabled=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.force=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# 카카오 OAuth2 설정
kakao.client-id=YOUR_KAKAO_CLIENT_ID
kakao.redirect-uri=http://localhost:8080/api/auth/kakao/callback
kakao.client-secret=
kakao.rest-key=d7b045eda53cfad08714855042e6034d
